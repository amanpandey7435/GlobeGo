
<% layout("/layouts/boilerplateCode") -%>

<style>
  .scrollable-filters {
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none; /* Firefox */
  }

  .scrollable-filters::-webkit-scrollbar {
    display: none; 
  }

  .icons {
    opacity: 0.7;
    font-size: 1.2rem;
    text-align: center;
    min-width: 5rem;
    flex-shrink: 0;
  }

  .icons:hover {
    opacity: 1;
    cursor: pointer;
    text-decoration: underline;
  }

  .tax-info {
    display: none;
  }

  .card-anchor {
    text-decoration: none;
    color: inherit;
  }

  .card img {
    object-fit: cover;
    width: 100%;
    height: 100%;
  }

  .tax-toggle {
    white-space: nowrap;
  }

  
  .scrollable-filters.active {
    cursor: grabbing;
    cursor: -webkit-grabbing;
  }
  .categories{
    text-decoration: none;
    color:black !important;
  }
</style>

<!-- Filters -->
<!-- <div id="filters" class="container-fluid mt-3">
  <div class="d-flex flex-wrap justify-content-start align-items-center gap-3 px-2">
   
    <div class="d-flex flex-row flex-nowrap scrollable-filters gap-3 px-1" style="max-width: 100%;">
      <div class="icons"><a href="/listings/categories/Trending" class="categories"><i class="fa-solid fa-fire"></i><p>Trending</p></a></div>
      <div class="icons"><a href="/listings/categories/Rooms" class="categories"><i class="fa-solid fa-bed"></i><p>Rooms</p></a></div>
      <div class="icons"><a href="/listings/categories/Cities" class="categories"><i class="fa-solid fa-city"></i><p>Iconic Cities</p></a></div>
      <div class="icons"><a href="/listings/categories/Mountains" class="categories"><i class="fa-solid fa-mountain-city"></i><p>Mountains</p></a></div>
      <div class="icons"><a href="/listings/categories/Castle" class="categories"><i class="fa-solid fa-building-wheat"></i><p>Castle</p></a></div>
      <div class="icons"><a href="/listings/categories/Arctics" class="categories"><i class="fa-solid fa-snowflake"></i><p>Arctics</p></a></div>
      <div class="icons"><a href="/listings/categories/Camping" class="categories"><i class="fa-solid fa-route"></i><p>Camping</p></a></div>
      <div class="icons"><a href="/listings/categories/Farms" class="categories"><i class="fa-solid fa-tractor"></i><p>Farms</p></a></div>
      <div class="icons"><a href="/listings/categories/Pools" class="categories"><i class="fa-solid fa-person-swimming"></i><p>Pools</p></a></div>
      <div class="icons"><a href="/listings/categories/Domes" class="categories"><i class="fa-solid fa-igloo"></i><p>Domes</p></a></div>
      <div class="icons"><a href="/listings/categories/Boats" class="categories"><i class="fa-solid fa-ship"></i><p>Boats</p></a></div>
      <div class="icons"><a href="/listings/categories/Temples" class="categories">	<i class="fas fa-bell"></i><p>Temples</p></a></div>
      
    </div>

   
    <div class="tax-toggle">
      <div class="form-check form-switch">
        <input class="form-check-input" type="checkbox" role="switch" id="flexSwitchCheckDefault" />
        <label class="form-check-label" for="flexSwitchCheckDefault">Display Taxes after Total</label>
      </div>
    </div>
  </div>
</div>


<div class="container mt-4">
  <div class="row row-cols-1 row-cols-sm-2 row-cols-md-2 row-cols-lg-3 g-4">
    <% for (let listing of listings) { %>
      <div class="col">
        <a href="/listings/<%= listing._id %>" class="card-anchor">
          <div class="card h-100 shadow-sm">
            <img src="<%= listing.image.url %>" class="card-img-top" alt="..." style="height: 18rem;" />
            <div class="card-body">
              <p class="card-text">
                <%= listing.title %> <br />
                &#8377;<%= listing.price.toLocaleString("en-IN") %>/night
                <i class="tax-info">&nbsp;&nbsp;+18% GST</i>
              </p>
            </div>
          </div>
        </a>
      </div>
    <% } %>
  </div>
</div>


<script>
  // Toggle GST info
  let taxSwitch = document.getElementById("flexSwitchCheckDefault");
  taxSwitch.addEventListener("click", () => {
    let taxInfo = document.getElementsByClassName("tax-info");
    for (let info of taxInfo) {
      info.style.display = (info.style.display !== 'inline') ? "inline" : "none";
    }
  });

  // Mouse drag scroll for filters
  const filterContainer = document.querySelector(".scrollable-filters");

  let isDown = false;
  let startX;
  let scrollLeft;

  filterContainer.addEventListener("mousedown", (e) => {
    isDown = true;
    filterContainer.classList.add("active");
    startX = e.pageX - filterContainer.offsetLeft;
    scrollLeft = filterContainer.scrollLeft;
  });

  filterContainer.addEventListener("mouseleave", () => {
    isDown = false;
    filterContainer.classList.remove("active");
  });

  filterContainer.addEventListener("mouseup", () => {
    isDown = false;
    filterContainer.classList.remove("active");
  });

  filterContainer.addEventListener("mousemove", (e) => {
    if (!isDown) return;
    e.preventDefault();
    const x = e.pageX - filterContainer.offsetLeft;
    const walk = (x - startX) * 2; // speed factor
    filterContainer.scrollLeft = scrollLeft - walk;
  });
</script> -->
<!-- Filters -->
<div id="filters" class="container-fluid mt-3">
  <div class="d-flex flex-wrap justify-content-start align-items-center gap-3 px-2">
    <div class="d-flex flex-row flex-nowrap scrollable-filters gap-3 px-1" style="max-width: 100%;">
      <!-- Filter Icons Scrollable -->
      <% const categories = [
        { name: "Trending", icon: "fa-fire" },
        { name: "Rooms", icon: "fa-bed" },
        { name: "Cities", icon: "fa-city" },
        { name: "Mountains", icon: "fa-mountain-city" },
        { name: "Castle", icon: "fa-building-wheat" },
        { name: "Arctics", icon: "fa-snowflake" },
        { name: "Camping", icon: "fa-route" },
        { name: "Farms", icon: "fa-tractor" },
        { name: "Pools", icon: "fa-person-swimming" },
        { name: "Domes", icon: "fa-igloo" },
        { name: "Boats", icon: "fa-ship" },
        { name: "Temples", icon: "fa-bell" }
      ]; %>

      <% categories.forEach(cat => { %>
        <div class="icons">
          <a href="/listings/categories/<%= cat.name %>" class="categories">
            <i class="fa-solid <%= cat.icon %>"></i>
            <p><%= cat.name %></p>
          </a>
        </div>
      <% }) %>
    </div>

    <!-- Tax Toggle -->
    <div class="tax-toggle">
      <div class="form-check form-switch">
        <input class="form-check-input" type="checkbox" role="switch" id="flexSwitchCheckDefault" />
        <label class="form-check-label" for="flexSwitchCheckDefault">Display Taxes after Total</label>
      </div>
    </div>
  </div>
</div>

<!-- Listings -->
<div class="container mt-4">
  <div class="row row-cols-1 row-cols-sm-2 row-cols-md-2 row-cols-lg-3 g-4">
    <% if (listings && listings.length > 0) { %>
      <% for (let listing of listings) { %>
        <% if (listing && listing.title && listing.price && listing.image && listing.image.url) { %>
          <div class="col">
            <a href="/listings/<%= listing._id %>" class="card-anchor">
              <div class="card h-100 shadow-sm">
                <% if (listing.image && listing.image.url) { %>
  <img src="<%= listing.image.url %>" class="card-img-top" alt="Listing image" style="height: 18rem;" />
<% } else { %>
  <img src="/images/default.jpg" class="card-img-top" alt="Default image" style="height: 18rem;" />
<% } %>

                <div class="card-body">
                  <p class="card-text">
                    <%= listing.title %> <br />
                    &#8377;<%= listing.price.toLocaleString("en-IN") %>/night
                    <i class="tax-info">&nbsp;&nbsp;+18% GST</i>
                  </p>
                </div>
              </div>
            </a>
          </div>
        <% } else { %>
          <!-- Incomplete listing data - skip or show fallback -->
        <% } %>
      <% } %>
    <% } else { %>
      <p>No listings available</p>
    <% } %>
  </div>
</div>

<!-- Tax Toggle & Scroll Script -->
<script>
  // Toggle GST info
  document.getElementById("flexSwitchCheckDefault").addEventListener("click", () => {
    const taxInfo = document.getElementsByClassName("tax-info");
    for (let info of taxInfo) {
      info.style.display = (info.style.display !== 'inline') ? "inline" : "none";
    }
  });

  // Drag-scroll filters
  const filterContainer = document.querySelector(".scrollable-filters");
  let isDown = false, startX, scrollLeft;

  filterContainer.addEventListener("mousedown", (e) => {
    isDown = true;
    filterContainer.classList.add("active");
    startX = e.pageX - filterContainer.offsetLeft;
    scrollLeft = filterContainer.scrollLeft;
  });
  filterContainer.addEventListener("mouseleave", () => isDown = false);
  filterContainer.addEventListener("mouseup", () => isDown = false);
  filterContainer.addEventListener("mousemove", (e) => {
    if (!isDown) return;
    e.preventDefault();
    const x = e.pageX - filterContainer.offsetLeft;
    const walk = (x - startX) * 2;
    filterContainer.scrollLeft = scrollLeft - walk;
  });
</script>